name: Apply complete reorganization

on:
  push:
    branches:
      - agent/reorganiza-nucleo-prospector
    paths:
      - scripts/apply_reorganization.py
      - scripts/.reorg_payload_*
      - .github/workflows/apply-reorganization.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Apply guarded refactor
        run: python scripts/apply_reorganization.py
      - name: Validate Python syntax
        run: python -m compileall -q app.py config.py core services ai database scraper whatsapp crm dashboard export tests
      - name: Install test dependencies
        run: pip install -r requirements-dev.txt
      - name: Run unit tests
        env:
          DATABASE_URL: postgresql://test:test@localhost:5432/test
          APP_TIMEZONE: America/Sao_Paulo
        run: pytest -q
      - name: Validate whitespace
        run: git diff --check
      - name: Commit generated changes
        run: |
          if git diff --quiet; then
            echo "No generated changes"
            exit 0
          fi
          git config user.name "prospector-repair-bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "refactor: harden reliability, security and runtime"
          git push
